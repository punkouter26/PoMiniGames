# .NET
bin/
obj/
*.dll
*.exe
*.pdb
*.user
*.suo
*.cache
*.log
.vs/
*.nupkg

# IDE
.idea/
.vscode/
*.swp
*.swo

# Build results
[Dd]ebug/
[Rr]elease/
x64/
x86/

# NuGet
*.nupkg
packages/
project.lock.json

# Test Results
TestResults/
coverage/
*.coverage
*.coveragexml

# Node
node_modules/
npm-debug.log
yarn-error.log

# Environment
.env
.env.local
.env.*.local
.env.development
.env.production

# OS
Thumbs.db
.DS_Store

# App specific
.potune-state.json

# Legacy React code (archived, has its own .git — keep out of main repo)
legacy-react/

# Deployment artifacts (generated by CI/CD — not source)
app-deploy.zip
appservice-logs.zip
publish/

# Agent / copilot configuration (machine-local, not repo source)
agentrc.config.json
agentrc.eval.json
cookbook/
instructions/
skills/

# LLM-generated docs (local only, not source-controlled)
LLMDOCS/

# E2E test dependencies (generated, not source)
tests/e2e/node_modules/
tests/e2e/package-lock.json
tests/e2e/playwright-report/
tests/e2e/.playwright/

# Playwright state
.auth/
playwright/.cache/

# Playwright MCP session data
.playwright-mcp/

# Test results / screenshots
test-results/
tests/e2e/test-results/
tests/e2e/*.png

# Adhoc test run screenshot directories
docs/screenshots/adhoc-*/

# Screenshot artifacts from e2e / debug sessions
docs/screenshots/*.png
docs/screenshots/*.jpg
docs/screenshots/*.jpeg
docs/screenshots/*.webp

# Data-protection key ring (Microsoft.AspNetCore.DataProtection) — written
# under <contentroot>/keys in Development. Contains XML-wrapped key material
# that MUST NOT be committed (would let anyone forge the BFF's cookies).
**/keys/

# Local SQLite data
src/PoMiniGames/PoMiniGames/data/

# Generated client artifacts
src/PoMiniGames.Client/dist/
src/PoMiniGames.Client/tsconfig.tsbuildinfo

# Vite build output served from wwwroot (produced by dotnet publish, not source)
src/PoMiniGames/PoMiniGames/wwwroot/assets/

# Runtime log files
src/PoMiniGames/PoMiniGames/logs/

# Language server / IDE caches
*.lscache

# Generated HTML exports from Mermaid diagrams (auto-generated, not source)
docs/*.html

# Copilot/agent session artifacts
.copilot-session/
agentrc.eval.json

# CI/CD publish artifacts (generated by GitHub Actions / dotnet publish)
publish/
publish-api/
api-deploy.zip
app-deploy.zip

# Root-level playwright config (moved to tests/e2e/)
package.json
package-lock.json
playwright.config.js

# SQLite runtime database
src/PoMiniGames/PoMiniGames/data/pominigames.db

# Playwright run metadata
tests/e2e/.last-run.json
playwright-report/
blob-report/

# Azure Developer CLI / local deployment state
.azd/
.azure/

# Additional test and build artifacts
artifacts/
*.trx
*.snap

# SQLite runtime files
*.sqlite3
*.db-shm
*.db-wal

# JetBrains Rider
*.sln.docstates
*.rsuser
*.DotSettings.user

# Backup and temporary files
*.orig
*.bak
*.tmp
*.swp

# Windows Thumbnails
Thumbs.db
ehthumbs.db

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Linux
.directory
.directory~
.hidden
.nfs*

# Build tool caches
.cmake/
CMakeCache.txt
