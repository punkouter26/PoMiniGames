C4Context
    title System Context — PoMiniGames (Azure westus2)
    Person(player, "Player", "Browser user — plays games, views leaderboards, joins real-time multiplayer")
    Person(dev, "Developer / Admin", "Deploys code, monitors telemetry, manages secrets via GitHub and azd")
    Enterprise_Boundary(azure, "Azure — rg-PoMiniGames + rg-PoShared") {
        System(swa, "Static Web App", "React 18 + TypeScript SPA served from Azure CDN global edge. Routes: 8 games, lobby, auth callback.")
        System(api, "App Service API (Linux B1)", ".NET 10 Minimal API + SignalR hubs. Plan: asp-poshared-linux. SystemAssigned Managed Identity.")
        SystemDb(sqlite, "SQLite (App Service disk)", "Unified pominigames.db at /home/data. Tables: PlayerStats, SnakeHighScores, PoDropSquareHighScores.")
        System_Ext(kv, "Key Vault (kv-poshared)", "Secrets: MicrosoftAuth--ClientId, MicrosoftAuth--ApiClientId, ApplicationInsights--ConnectionString.")
        System_Ext(appins, "Application Insights", "OpenTelemetry sink. Receives traces, logs, metrics, exceptions from the API.")
    }
    System_Ext(msid, "Microsoft Identity Platform", "OAuth2 / OIDC provider. Issues JWT access tokens. Validates Bearer tokens on API.")
    System_Ext(gh, "GitHub Actions", "CI/CD pipeline. Triggers on master push. Builds .NET + React, runs tests, deploys to Azure via OIDC federation.")
    Rel(player, swa, "Plays games, views leaderboards", "HTTPS")
    Rel(player, api, "Real-time multiplayer moves", "WSS / SignalR")
    Rel(swa, api, "REST calls + SignalR handshake", "HTTPS / WSS")
    Rel(api, sqlite, "Read/write player stats and scores", "SQLite ADO.NET")
    Rel(api, msid, "Validate JWT Bearer tokens", "HTTPS")
    Rel(api, kv, "Fetch secrets at startup", "HTTPS + Managed Identity")
    Rel(api, appins, "Emit OpenTelemetry telemetry", "HTTPS")
    Rel(dev, gh, "Push to master branch", "Git")
    Rel(gh, swa, "Deploy React bundle", "HTTPS — SWA deployment token")
    Rel(gh, api, "Zip-deploy .NET binaries", "HTTPS — OIDC federation")
