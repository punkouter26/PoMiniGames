{
  "name": "pominigames-js",
  "private": true,
  "type": "module",
  "description": "JS-side tooling for PoMiniGames: Vitest for the PoEcosystem simulation (tests/PoEcosystem.Sim) and ESLint for wwwroot/js/poecosystem. The Blazor solution is built with dotnet; this package never bundles anything.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint --max-warnings 0 --no-error-on-unmatched-pattern \"src/PoMiniGames.Client/wwwroot/js/poecosystem/**/*.js\" \"tests/PoEcosystem.Sim/**/*.js\""
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@vitest/coverage-v8": "4.1.11",
    "cannon-es": "0.20.0",
    "eslint": "10.9.1",
    "globals": "17.12.0",
    "vitest": "4.1.11"
  },
  "engines": {
    "node": ">=20"
  }
}
