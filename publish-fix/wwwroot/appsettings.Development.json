{
  "//": "ApiBaseAddress intentionally unset: the host serves the WASM and the API on the same origin (no separate frontend server), so the client uses its served origin as the API base. Set PoMiniGames:ApiBaseAddress here only for a split dev-server setup where the WASM is served from a different origin than the API.",
  "PoMiniGames": {}
}
