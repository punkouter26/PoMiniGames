<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- TFM / Nullable / ImplicitUsings inherited from Directory.Build.props.
         §1.7 Trimming Safeguards. Kept on after PoSurvive's removal (2026-09-12) took
         this project out of the WASM payload: it is cheap, and the analyzer is the thing
         that would catch a reflection-heavy addition here before a client ever
         referenced it again. -->
    <IsTrimmable>true</IsTrimmable>
    <EnableTrimAnalyzer>true</EnableTrimAnalyzer>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\PoMiniGames.Domain\PoMiniGames.Domain.csproj" />
    <ProjectReference Include="..\PoMiniGames.Shared\PoMiniGames.Shared.csproj" />
  </ItemGroup>
</Project>