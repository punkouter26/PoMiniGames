{
  "//": "Loaded by the WASM client when the host runs under the 'Test' environment (the E2E-UI tier). Blazor reads appsettings.json then appsettings.{Environment}.json from wwwroot, with the environment coming from the host's blazor-environment header."
}
